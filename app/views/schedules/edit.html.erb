<% content_for :title, "Editing schedule" %>

<h1><%= t("header.edit_schedule") %></h1>

<%= render "form", schedule: @schedule %>

<br>

<div>
  <%= link_to t(".show_schedule"), @schedule %> |
  <%= link_to t(".back_schedule_index"), schedules_path %>
</div>
